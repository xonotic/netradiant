# Crunch

Only those header files are needed:

- `crnlib.h`
- `crn_decomp.h`
- `crn_defs.h`

Upstream:

- https://github.com/DaemonEngine/crunch

The headers can be found in the `inc/` directory of the repository.

The files are meant to be updated with the `library-updater` script.
